environment: development

server:
  port: 50051
  metrics_port: 9090

# Note: This example uses fake data, so database/redis are not needed
# In a real application, you would configure these:
# database:
#   host: localhost
#   port: 5432
#   database: demo_db
#   user: postgres
#   password: ${DB_PASSWORD}
#
# redis:
#   host: localhost
#   port: 6379
#   db: 0

logging:
  level: debug

